org.gradle.jvmargs=-Xmx3072m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
android.useAndroidX=true
android.nonTransitiveRClass=true
# AGP does not run on JDK 25. Point JAVA_HOME at a JDK 21 before invoking gradlew,
# or set org.gradle.java.home in your own ~/.gradle/gradle.properties (machine-local,
# since the path is host-specific).
