[package]
name = "oura-summary"
description = "The dashboard summary computation, shared by the web (oura-cli) and native (oura-core) clients"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
oura-protocol = { git = "https://github.com/Th0rgal/open_oura", branch = "web-dashboard" }
oura-store = { git = "https://github.com/Th0rgal/open_oura", branch = "web-dashboard" }
oura-analysis = { git = "https://github.com/Th0rgal/open_oura", branch = "web-dashboard" }
serde_json = "1"
anyhow = "1"
