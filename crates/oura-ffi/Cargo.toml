[package]
name = "oura-ffi"
description = "C-ABI surface over the Oura protocol core for the native iOS app"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lib]
name = "ouraffi"
crate-type = ["staticlib"]

[dependencies]
oura-protocol = { git = "https://github.com/Th0rgal/open_oura", branch = "split-open-health" }
serde_json = "1"
